# Copy to .env and fill in real values. Never commit .env.

# Twilio (https://console.twilio.com)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# ElevenLabs Conversational AI (CallPilot backend)
ELEVENLABS_API_KEY=your_elevenlabs_api_key
AGENT_ID=your_agent_id
AGENT_PHONE_NUMBER_ID=your_agent_phone_number_id
JESSICA_VOICE_ID=cgSgspJ2msm6clMCkdW9

# OpenAI (for refine_instruction / Jessica briefing)
OPENAI_API_KEY=your_openai_api_key

# Booking webhook: ElevenLabs confirm_booking tool should POST to your backend URL + /webhook/booking
# Make.com (optional): custom webhook URL; backend forwards each booking here
MAKE_WEBHOOK_URL=https://hook.eu1.make.com/xxxx

# Google Calendar API (optional): path to service account JSON
# - For status/availability: use read-only scope. For saving dates from webhook: use calendar.events (write).
# - With a service account, share your target calendar with the SA email and set GOOGLE_CALENDAR_ID to that calendar's ID (not "primary").
# GOOGLE_CALENDAR_CREDENTIALS_PATH=./credentials/calendar-service-account.json
# GOOGLE_CALENDAR_ID=primary
